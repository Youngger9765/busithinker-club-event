<div class="form-group">
  <%= f.label :name ,"社團名稱"%>
  <%= f.text_field :name, :class => "form-control" %>
</div>

<br> 


<div class="form-group">
  <%=f.label :banner, "社團banner(底圖)"%>
  <br>
  <% if @admin_club.banner.exists? %>
    <%= image_tag(@admin_club.banner.url(:medium))%>
  <%else%>
    <div style="color:red;">
      <%= "尚未有 banner 圖片"%>
    </div>  
  <% end %>
  <%=f.file_field :banner%>
  
  <% if @admin_club.banner.exists? %>
    <%= check_box_tag "destroy_banner", "1" %> 刪除檔案
  <% end %>
</div>



<div class="form-group">
    <%=f.label :logo, "社團 logo（小圖）"%>
    <br>
    <% if @admin_club.logo.exists? %>
      <%= image_tag(@admin_club.logo.url(:medium))%>
    <%else%>
      <div style="color:red;">
        <%= "尚未有 logo 圖片"%>
      </div>  
    <% end %>
    <%=f.file_field :logo%>
    
    <% if @admin_club.logo.exists? %>
      <%= check_box_tag "destroy_logo", "1" %> 刪除檔案
    <% end %>
</div>

<%= f.submit "更新",:class => "btn btn-primary" %>

<hr>

<script>
  $(function() {
    $('.froala-editor').froalaEditor({
      imageUploadURL: '/attachment/upload.json',
      imageUploadMethod: 'POST'
    });
  });
</script>